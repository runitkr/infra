apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd
  namespace: admin
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - argo.admin.run.it.kr
    secretName: argocd-ingress-tls
  rules:
  - host: argo.admin.run.it.kr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 8080
